export const locales = {
    logs: {
        initializing: 'graphql initializing...'
    },
    userMessaging: {
        getGreeting: (name) => {
            return `welcome to the app ${name}`
        }
    },
};
